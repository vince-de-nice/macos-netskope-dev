name: Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install shellcheck
        run: brew install shellcheck

      - name: Smoke install dry-run
        run: ./install.sh --all --netskope --dry-run --yes

      - name: shellcheck
        run: shellcheck -S warning -x install.sh lib/*.sh scripts/*.sh test/run-tests.sh

      - name: Run test suite
        run: ./test/run-tests.sh
